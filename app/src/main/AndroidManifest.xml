<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.main">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".io_storage.IoMsgActivity" />
        <activity android:name=".io_storage.IoInsertActivity" />
        <activity android:name=".goods.AddActivity" />
        <activity android:name=".goods.FindResultActivity" />
        <activity android:name=".goods.GoodsMsgActivity" />
        <activity android:name=".goods.UpdateGoodsActivity" />
        <activity android:name=".storage_manage.AddStoActivity" />
        <activity android:name=".storage_manage.FindStoActivity" />
        <activity android:name=".storage_manage.InfoStoActivity" />
        <activity android:name=".storage_manage.UpdateStoActivity" />
        <activity
            android:name=".yuangong.yuangongMsgActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".yuangong.FindResultActitivity"
            android:label="@string/add_goods"
            android:theme="@android:style/Theme.Dialog">

        </activity>

        <activity
            android:name=".yuangong.AddyuangongActivity"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".yuangong.UpdateyuangongActivity"
            android:label="@string/app_name">

        </activity>

    </application>

</manifest>